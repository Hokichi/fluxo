<UserControl
    x:Class="Fluxo.Views.Shell.Wizard.Pages.Steps.Notification"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:coreEnums="clr-namespace:Fluxo.Core.Enums;assembly=Fluxo.Core"
    xmlns:customControls="clr-namespace:Fluxo.Resources.CustomControls;assembly=Fluxo.Resources"
    xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework">
    <UserControl.Resources>
        <DataTemplate x:Key="WizardNotificationSettingItemTemplate">
            <Border Margin="0,0,0,12" Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock
                            FontFamily="{StaticResource Bold}"
                            Foreground="{DynamicResource Brush.Text.Primary}"
                            Text="{Binding Title}" />
                        <TextBlock
                            Margin="0,6,0,0"
                            FontFamily="{StaticResource Medium}"
                            Foreground="{DynamicResource Brush.Text.Secondary}"
                            Text="{Binding Description}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                    <ToggleButton
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        IsChecked="{Binding IsEnabled, Mode=TwoWay}"
                        Style="{StaticResource ToggleSwitchStyle}" />
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <!--  Step 7: Notifications  -->
    <StackPanel Visibility="{Binding IsStep7Active, Converter={StaticResource BoolToVisibilityConverter}}">
        <Border Margin="0,0,0,12" Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock
                            FontFamily="{StaticResource Bold}"
                            Foreground="{DynamicResource Brush.Text.Primary}"
                            Text="Run with Windows" />
                        <StackPanel Margin="0,6,0,0" Orientation="Horizontal">
                            <TextBlock
                                FontFamily="{StaticResource Medium}"
                                Foreground="{DynamicResource Brush.Text.Secondary}"
                                TextWrapping="Wrap">
                                <Run Text="Start " />
                            </TextBlock>
                            <TextBlock
                                FontFamily="{StaticResource Bold}"
                                Foreground="{DynamicResource Brush.Text.Secondary}"
                                Text="flux" />
                            <TextBlock
                                FontFamily="{StaticResource Bold}"
                                Foreground="{DynamicResource Brush.Mint.Muted}"
                                Text="o" />
                            <TextBlock
                                FontFamily="{StaticResource Medium}"
                                Foreground="{DynamicResource Brush.Text.Secondary}"
                                TextWrapping="Wrap">
                                <Run Text=" in the system tray when Windows signs in." />
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                    <ToggleButton
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        IsChecked="{Binding ShouldRunAtStartup, Mode=TwoWay}"
                        Style="{StaticResource ToggleSwitchStyle}" />
                </Grid>
                <Grid Margin="0,12,0,0" VerticalAlignment="Center">
                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                        <TextBlock
                            FontFamily="{StaticResource Bold}"
                            Foreground="{DynamicResource Brush.Text.Primary}"
                            Text="When closing " />
                        <TextBlock
                            FontFamily="{StaticResource Bold}"
                            Foreground="{DynamicResource Brush.Text.Primary}"
                            Text="flux" />
                        <TextBlock
                            FontFamily="{StaticResource Bold}"
                            Foreground="{DynamicResource Brush.Mint}"
                            Text="o" />
                    </StackPanel>
                    <customControls:SegmentedToggleGroup Width="252" HorizontalAlignment="Right">
                        <customControls:SegmentedToggleOption
                            Command="{Binding SetCloseBehaviorCommand}"
                            CommandParameter="{x:Static coreEnums:AppCloseBehavior.MinimizeToTray}"
                            Content="Minimize to tray"
                            IsSelected="{Binding IsMinimizeToTrayCloseBehaviorSelected, Mode=OneWay}"
                            SelectOnClick="False" />
                        <customControls:SegmentedToggleOption
                            Command="{Binding SetCloseBehaviorCommand}"
                            CommandParameter="{x:Static coreEnums:AppCloseBehavior.Exit}"
                            Content="Exit"
                            IsSelected="{Binding IsExitCloseBehaviorSelected, Mode=OneWay}"
                            SelectOnClick="False" />
                    </customControls:SegmentedToggleGroup>
                </Grid>
            </StackPanel>
        </Border>

        <ItemsControl ItemTemplate="{StaticResource WizardNotificationSettingItemTemplate}" ItemsSource="{Binding NotificationSettings}" />
    </StackPanel>

</UserControl>
