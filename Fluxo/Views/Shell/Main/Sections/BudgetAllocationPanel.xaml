<UserControl
    x:Class="Fluxo.Views.Shell.Main.Sections.BudgetAllocationPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="clr-namespace:Fluxo.Resources.Components;assembly=Fluxo.Resources"
    xmlns:customControls="clr-namespace:Fluxo.Resources.CustomControls;assembly=Fluxo.Resources"
    xmlns:enums="clr-namespace:Fluxo.Core.Enums;assembly=Fluxo.Core">
    <UserControl.Resources>
        <ItemsPanelTemplate x:Key="BudgetAllocationTagItemsPanelTemplate">
            <StackPanel Orientation="Horizontal" />
        </ItemsPanelTemplate>

        <ItemsPanelTemplate x:Key="BudgetAllocationPopupTagItemsPanelTemplate">
            <WrapPanel />
        </ItemsPanelTemplate>

        <Style x:Key="BudgetAllocationVisibleTagListViewItemStyle" BasedOn="{StaticResource BaseListViewItemStyle}" TargetType="ListViewItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BudgetAllocationTransactionsListStyle" BasedOn="{StaticResource BaseTransactionsListStyle}" TargetType="components:TransactionsList">
            <Setter Property="IsEmptyActionVisible" Value="True" />
            <Setter Property="MaxVisibleItems" Value="5" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding DataContext.IsSufficientFundsActionGateLocked, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Value="True">
                    <Setter Property="IsEmptyActionVisible" Value="False" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsWindowLayoutMaximized, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Value="True">
                    <Setter Property="MaxVisibleItems" Value="8" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="1"
            Height="32"
            Margin="0,0,0,16"
            Orientation="Horizontal">
            <Grid Margin="16,0,0,0" VerticalAlignment="Center">
                <Border
                    x:Name="AllTagUnselected"
                    Height="24"
                    Padding="12,0"
                    Background="{StaticResource Brush.Background.Surface}"
                    BorderBrush="{StaticResource Brush.Border.Subtle}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Cursor="Hand">
                    <Border.Style>
                        <Style BasedOn="{StaticResource BaseBorderStyle}" TargetType="Border">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedTag}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Border.InputBindings>
                        <MouseBinding Command="{Binding ClearSelectedTagCommand}" MouseAction="LeftClick" />
                    </Border.InputBindings>
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource Bold}"
                        Foreground="{StaticResource Brush.Text.Primary}"
                        Text="All" />
                </Border>

                <Border
                    x:Name="AllTagSelected"
                    Height="24"
                    Padding="12,0"
                    Background="{StaticResource Brush.Mint}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Cursor="Hand">
                    <Border.Style>
                        <Style BasedOn="{StaticResource BaseBorderStyle}" TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedTag}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Border.InputBindings>
                        <MouseBinding Command="{Binding ClearSelectedTagCommand}" MouseAction="LeftClick" />
                    </Border.InputBindings>
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource Bold}"
                        Foreground="{StaticResource Brush.Background.Surface}"
                        Text="All" />
                </Border>
            </Grid>

            <ListView
                VerticalAlignment="Center"
                Background="Transparent"
                BorderThickness="0"
                ItemContainerStyle="{StaticResource BudgetAllocationVisibleTagListViewItemStyle}"
                ItemTemplate="{StaticResource Tags}"
                ItemsPanel="{StaticResource BudgetAllocationTagItemsPanelTemplate}"
                ItemsSource="{Binding Tags}"
                PreviewMouseLeftButtonDown="OnTagListPreviewMouseLeftButtonDown"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                SelectedItem="{Binding SelectedVisibleTag}" />

            <ToggleButton
                x:Name="MoreTagsButton"
                Margin="16,0,0,0"
                VerticalAlignment="Center"
                Content="More"
                Style="{StaticResource TagToggleButtonStyle}"
                Tag="{Binding IsSelectedTagInOtherTags}"
                Visibility="{Binding HasOtherTags, Converter={StaticResource BoolToVisibilityConverter}}" />

            <Popup
                AllowsTransparency="True"
                DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}"
                IsOpen="{Binding IsChecked, ElementName=MoreTagsButton, Mode=TwoWay}"
                Placement="Bottom"
                PlacementTarget="{Binding ElementName=MoreTagsButton}"
                PopupAnimation="Fade"
                StaysOpen="False"
                VerticalOffset="8">
                <Border
                    Padding="12"
                    Background="{StaticResource Brush.Background.Elevated}"
                    BorderBrush="{StaticResource Brush.Border.Subtle}"
                    BorderThickness="1"
                    CornerRadius="16">
                    <Grid MaxWidth="320">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock
                            FontFamily="{StaticResource Bold}"
                            FontSize="12"
                            Foreground="{StaticResource Brush.Text.Primary}"
                            Text="More tags" />

                        <customControls:FadingScrollViewer
                            Grid.Row="2"
                            MaxHeight="240"
                            HorizontalScrollBarVisibility="Disabled"
                            VerticalScrollBarVisibility="Auto">
                            <ListView
                                Background="Transparent"
                                BorderThickness="0"
                                ItemContainerStyle="{StaticResource PopupTagListViewItemStyle}"
                                ItemTemplate="{StaticResource PopupTags}"
                                ItemsPanel="{StaticResource BudgetAllocationPopupTagItemsPanelTemplate}"
                                ItemsSource="{Binding OtherTags}"
                                PreviewMouseLeftButtonDown="OnTagListPreviewMouseLeftButtonDown"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                SelectedItem="{Binding SelectedOtherTag}"
                                SelectionChanged="OnMoreTagsSelectionChanged" />
                        </customControls:FadingScrollViewer>
                    </Grid>
                </Border>
            </Popup>
        </StackPanel>

        <Grid Grid.Row="2">
            <components:TransactionsList
                Title="NEEDS"
                DeleteCommand="{Binding DeleteExpenseLogCommand}"
                DotColor="{StaticResource Brush.Category.Needs}"
                EmptyActionText="No transaction found"
                IsEmptyActionVisible="False"
                IsListEmpty="{Binding IsTransactionsEmpty}"
                ItemsSource="{Binding Transactions}"
                Style="{StaticResource BudgetAllocationTransactionsListStyle}" />
        </Grid>
    </Grid>
</UserControl>
