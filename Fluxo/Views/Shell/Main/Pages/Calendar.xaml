<UserControl
    x:Class="Fluxo.Views.Shell.Main.Pages.Calendar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:customControls="clr-namespace:Fluxo.Resources.CustomControls;assembly=Fluxo.Resources"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:main="clr-namespace:Fluxo.ViewModels.Shell.Main"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="CalendarRoot"
    d:DataContext="{d:DesignInstance Type=main:CalendarVM}"
    d:DesignHeight="810"
    d:DesignWidth="1440"
    Focusable="True"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="CalendarItemDetailTextStyle" BasedOn="{StaticResource BaseTextBlockStyle}" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,3,0,0" />
            <Setter Property="FontFamily" Value="{StaticResource Medium}" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}" />
        </Style>

        <Style x:Key="CalendarItemTitleTextStyle" BasedOn="{StaticResource BaseTextBlockStyle}" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource Bold}" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
        </Style>

        <Style x:Key="CalendarDayNumberTextStyle" BasedOn="{StaticResource BaseTextBlockStyle}" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary.Inverted}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <ItemsPanelTemplate x:Key="CalendarWeekItemsPanelTemplate">
            <UniformGrid Columns="7" Rows="1" />
        </ItemsPanelTemplate>

        <Style x:Key="CalendarColumnLayoutStyle" BasedOn="{StaticResource BaseGridStyle}" TargetType="Grid">
            <Setter Property="Width" Value="480" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding UseExpandedCalendarLayout, ElementName=CalendarRoot}" Value="True">
                    <Setter Property="Width" Value="570" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style
            x:Key="CalendarGridCardStyle"
            BasedOn="{StaticResource CardBorderStyle}"
            TargetType="Border">
            <Setter Property="Width" Value="480" />
            <Setter Property="Height" Value="480" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding UseExpandedCalendarLayout, ElementName=CalendarRoot}" Value="True">
                    <Setter Property="Width" Value="570" />
                    <Setter Property="Height" Value="570" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CalendarSectionTitleStyle" BasedOn="{StaticResource BaseTextBlockStyle}" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource Bold}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
        </Style>

        <Style x:Key="CalendarCardLabelStyle" BasedOn="{StaticResource CardLabelTextStyle}" TargetType="TextBlock" />

        <Style x:Key="CalendarListItemHoverBorderStyle" BasedOn="{StaticResource BaseBorderStyle}" TargetType="Border">
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="CornerRadius" Value="10" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Brush.Background.Hover}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CalendarListItemContainerStyle" BasedOn="{StaticResource BaseContentPresenterStyle}" TargetType="ContentPresenter">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
        </Style>

        <Style x:Key="CalendarListItemsControlStyle" BasedOn="{StaticResource BaseItemsControlStyle}" TargetType="ItemsControl">
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <StaticResource ResourceKey="CalendarListItemContainerStyle" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style
            x:Key="CalendarWeekHeaderLabelStyle"
            BasedOn="{StaticResource CalendarCardLabelStyle}"
            TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>

        <Style x:Key="CalendarValueTextStyle" BasedOn="{StaticResource BaseTextBlockStyle}" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,6,0,0" />
            <Setter Property="FontFamily" Value="{StaticResource Bold}" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
        </Style>

        <Style
            x:Key="CalendarSummaryInlineValueTextStyle"
            BasedOn="{StaticResource CalendarValueTextStyle}"
            TargetType="TextBlock">
            <Setter Property="Margin" Value="12,0,0,0" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="HorizontalAlignment" Value="Right" />
        </Style>

        <Style x:Key="CalendarNoDataTextStyle" BasedOn="{StaticResource CenteredEmptyStateTextStyle}" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <ControlTemplate x:Key="CalendarDayButtonTemplate" TargetType="Button">
            <Border
                x:Name="DayBorder"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="10">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="DayBorder" Property="Background" Value="{DynamicResource Brush.Background.Hover}" />
                </Trigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsSelected}" Value="True" />
                        <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource TemplatedParent}}" Value="True" />
                    </MultiDataTrigger.Conditions>
                    <Setter TargetName="DayBorder" Property="Background" Value="{DynamicResource Brush.Mint.Muted}" />
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsCurrentDay}" Value="True" />
                        <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource TemplatedParent}}" Value="True" />
                    </MultiDataTrigger.Conditions>
                    <Setter TargetName="DayBorder" Property="BorderBrush" Value="{DynamicResource Brush.Mint.Muted}" />
                </MultiDataTrigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="CalendarDayButtonStyle" BasedOn="{StaticResource BaseButtonStyle}" TargetType="Button">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="MinHeight" Value="42" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Default}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <StaticResource ResourceKey="CalendarDayButtonTemplate" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Brush.Mint}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Mint}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsCurrentDay}" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Mint}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsOutsideVisibleMonth}" Value="True">
                    <Setter Property="Opacity" Value="0.45" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <DataTemplate x:Key="CalendarDayTemplate">
            <Button
                x:Name="CalendarDayButton"
                Click="OnCalendarDayButtonClick"
                Command="{Binding DataContext.SelectDateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                CommandParameter="{Binding}"
                Focusable="False"
                Style="{StaticResource CalendarDayButtonStyle}">
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontFamily="{StaticResource Bold}"
                    FontSize="14"
                    Text="{Binding DayNumber}" Style="{StaticResource CalendarDayNumberTextStyle}">

                </TextBlock>
            </Button>
        </DataTemplate>

        <DataTemplate x:Key="CalendarWeekTemplate">
            <ItemsControl
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                ItemTemplate="{StaticResource CalendarDayTemplate}"
                ItemsSource="{Binding Days}" ItemsPanel="{StaticResource CalendarWeekItemsPanelTemplate}">

            </ItemsControl>
        </DataTemplate>

        <DataTemplate x:Key="ExpenseItemTemplate">
            <Border Style="{StaticResource CalendarListItemHoverBorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Style="{StaticResource CalendarItemTitleTextStyle}"
                            Text="{Binding Name}" />
                        <TextBlock Style="{StaticResource CalendarItemDetailTextStyle}"
                            Text="{Binding AccountName}" />
                    </StackPanel>
                    <TextBlock
                        Grid.Column="1"
                        FontFamily="{StaticResource Bold}"
                        Foreground="{DynamicResource Brush.Danger}"
                        Text="{Binding AmountText}" />
                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="IncomeItemTemplate">
            <Border Style="{StaticResource CalendarListItemHoverBorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Style="{StaticResource CalendarItemTitleTextStyle}"
                            Text="{Binding Name}" />
                        <TextBlock Style="{StaticResource CalendarItemDetailTextStyle}"
                            Text="{Binding AccountName}" />
                    </StackPanel>
                    <TextBlock
                        Grid.Column="1"
                        FontFamily="{StaticResource Bold}"
                        Foreground="{DynamicResource Brush.Mint}"
                        Text="{Binding AmountText}" />
                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="GoalDeadlineItemTemplate">
            <Border Style="{StaticResource CalendarListItemHoverBorderStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CalendarItemTitleTextStyle}"
                        Text="{Binding Name}" />
                    <TextBlock Style="{StaticResource CalendarItemDetailTextStyle}"
                        Text="{Binding ProgressText}" />
                </StackPanel>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="RecurringTransactionItemTemplate">
            <Border Style="{StaticResource CalendarListItemHoverBorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Style="{StaticResource CalendarItemTitleTextStyle}"
                            Text="{Binding Name}" />
                        <TextBlock Style="{StaticResource CalendarItemDetailTextStyle}">
                            <Run Text="{Binding TypeText}" />
                            <Run Text=" - " />
                            <Run Text="{Binding SourceName}" />
                        </TextBlock>
                    </StackPanel>
                    <TextBlock
                        Grid.Column="1"
                        FontFamily="{StaticResource Bold}"
                        Foreground="{DynamicResource Brush.Info}"
                        Text="{Binding AmountText}" />
                </Grid>
            </Border>
        </DataTemplate>

<ItemsPanelTemplate x:Key="CalendarBufferedWeeksItemsPanelTemplate">
                                        <UniformGrid Rows="10" />
                                    </ItemsPanelTemplate>

<Style BasedOn="{StaticResource BaseContentPresenterStyle}" TargetType="ContentPresenter" x:Key="CalendarWeekItemContainerStyle">
                                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                                        <Setter Property="VerticalAlignment" Value="Stretch" />
                                    </Style>
</UserControl.Resources>

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="14" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Style="{StaticResource CalendarColumnLayoutStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Style="{StaticResource CalendarItemTitleTextStyle}"
                        VerticalAlignment="Center"
                        FontSize="18"
                        Text="{Binding VisibleMonthLabel}" />
                    <StackPanel
                        Grid.Column="1"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                        <customControls:BalloonButton
                            ButtonIcon="{StaticResource AngleUp}"
                            Command="{Binding NavigateToPreviousMonthCommand}"
                            DefaultBackground="{DynamicResource Brush.BalloonButton.Background.Surface}"
                            HoveredBackground="{DynamicResource Brush.BalloonButton.Background.Surface.Hovered}"
                            Style="{StaticResource ToolbarActionBalloonButtonStyle}"
                            ToolTip="Previous month" />
                        <customControls:BalloonButton
                            ButtonIcon="{StaticResource CalendarTodayOutline}"
                            Click="OnCalendarTodayButtonClick"
                            DefaultBackground="{DynamicResource Brush.BalloonButton.Background.NavInfo}"
                            HoveredBackground="{DynamicResource Brush.BalloonButton.Background.NavInfo.Hovered}"
                            IsEnabled="{Binding IsAtCurrentDay, Converter={StaticResource BoolNegationConverter}}"
                            Style="{StaticResource ToolbarActionBalloonButtonStyle}"
                            ToolTip="Today" />
                        <customControls:BalloonButton
                            ButtonIcon="{StaticResource AngleDown}"
                            Command="{Binding NavigateToNextMonthCommand}"
                            DefaultBackground="{DynamicResource Brush.BalloonButton.Background.Surface}"
                            HoveredBackground="{DynamicResource Brush.BalloonButton.Background.Surface.Hovered}"
                            Style="{StaticResource ToolbarActionBalloonButtonStyle}"
                            ToolTip="Next month" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border
                Grid.Row="1"
                Margin="0,12,0,0"
                MouseWheel="OnCalendarMouseWheel"
                Style="{StaticResource CalendarGridCardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <UniformGrid Columns="7">
                        <TextBlock Style="{StaticResource CalendarWeekHeaderLabelStyle}" Text="SUN" />
                        <TextBlock Style="{StaticResource CalendarWeekHeaderLabelStyle}" Text="MON" />
                        <TextBlock Style="{StaticResource CalendarWeekHeaderLabelStyle}" Text="TUE" />
                        <TextBlock Style="{StaticResource CalendarWeekHeaderLabelStyle}" Text="WED" />
                        <TextBlock Style="{StaticResource CalendarWeekHeaderLabelStyle}" Text="THU" />
                        <TextBlock Style="{StaticResource CalendarWeekHeaderLabelStyle}" Text="FRI" />
                        <TextBlock Style="{StaticResource CalendarWeekHeaderLabelStyle}" Text="SAT" />
                    </UniformGrid>

                    <Grid
                        x:Name="CalendarWeekViewport"
                        Grid.Row="1"
                        Margin="0,10,0,0"
                        ClipToBounds="True"
                        Focusable="True"
                        SizeChanged="OnCalendarWeekViewportSizeChanged">
                        <Canvas x:Name="CalendarWeekSurface" ClipToBounds="False">
<ItemsControl
                                x:Name="CalendarWeekItemsControl"
                                Width="{Binding ActualWidth, ElementName=CalendarWeekViewport}"
                                ItemTemplate="{StaticResource CalendarWeekTemplate}"
                                ItemsSource="{Binding BufferedWeeks}" ItemsPanel="{StaticResource CalendarBufferedWeeksItemsPanelTemplate}" ItemContainerStyle="{StaticResource CalendarWeekItemContainerStyle}">
                                <ItemsControl.RenderTransform>
                                    <TranslateTransform x:Name="CalendarWeekTranslateTransform" />
                                </ItemsControl.RenderTransform>


                            </ItemsControl>
                        </Canvas>
                    </Grid>
                </Grid>
            </Border>

            <Grid Grid.Row="3">
                <Grid
                    Style="{StaticResource BaseGridStyle}"
                    Visibility="{Binding UseExpandedCalendarLayout, ElementName=CalendarRoot, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Style="{StaticResource CardBorderStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource CalendarCardLabelStyle}"
                                Text="Total Spent" />
                            <TextBlock
                                Grid.Column="1"
                                Foreground="{DynamicResource Brush.Danger}"
                                Style="{StaticResource CalendarSummaryInlineValueTextStyle}"
                                Text="{Binding TotalSpentText}" />
                        </Grid>
                    </Border>

                    <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource CalendarCardLabelStyle}"
                                Text="Total Earned" />
                            <TextBlock
                                Grid.Column="1"
                                Foreground="{DynamicResource Brush.Mint}"
                                Style="{StaticResource CalendarSummaryInlineValueTextStyle}"
                                Text="{Binding TotalEarnedText}" />
                        </Grid>
                    </Border>

                    <Border Grid.Row="4" Style="{StaticResource CardBorderStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource CalendarCardLabelStyle}"
                                Text="Goals Due" />
                            <TextBlock
                                Grid.Column="1"
                                Foreground="{DynamicResource Brush.Warning}"
                                Style="{StaticResource CalendarSummaryInlineValueTextStyle}"
                                Text="{Binding GoalsDueText}" />
                        </Grid>
                    </Border>

                    <Border Grid.Row="6" Style="{StaticResource CardBorderStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource CalendarCardLabelStyle}"
                                Text="Payment Due" />
                            <TextBlock
                                Grid.Column="1"
                                Foreground="{DynamicResource Brush.Info}"
                                Style="{StaticResource CalendarSummaryInlineValueTextStyle}"
                                Text="{Binding PaymentDueText}" />
                        </Grid>
                    </Border>
                </Grid>

                <Grid
                    Style="{StaticResource BaseGridStyle}"
                    Visibility="{Binding UseExpandedCalendarLayout, ElementName=CalendarRoot, Mode=OneWay, Converter={StaticResource BoolToVisibilityInvertedConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border
                        Padding="16,0"
                        HorizontalAlignment="Stretch"
                        Style="{StaticResource CardBorderStyle}">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Style="{StaticResource CalendarCardLabelStyle}" Text="Total Spent" />
                            <TextBlock
                                Foreground="{DynamicResource Brush.Danger}"
                                Style="{StaticResource CalendarValueTextStyle}"
                                Text="{Binding TotalSpentText}" />
                        </StackPanel>
                    </Border>

                    <Border
                        Grid.Column="2"
                        Padding="16,0"
                        HorizontalAlignment="Stretch"
                        Style="{StaticResource CardBorderStyle}">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Style="{StaticResource CalendarCardLabelStyle}" Text="Total Earned" />
                            <TextBlock
                                Foreground="{DynamicResource Brush.Mint}"
                                Style="{StaticResource CalendarValueTextStyle}"
                                Text="{Binding TotalEarnedText}" />
                        </StackPanel>
                    </Border>

                    <Border
                        Grid.Row="2"
                        Padding="16,0"
                        HorizontalAlignment="Stretch"
                        Style="{StaticResource CardBorderStyle}">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Style="{StaticResource CalendarCardLabelStyle}" Text="Goals Due" />
                            <TextBlock
                                Foreground="{DynamicResource Brush.Warning}"
                                Style="{StaticResource CalendarValueTextStyle}"
                                Text="{Binding GoalsDueText}" />
                        </StackPanel>
                    </Border>

                    <Border
                        Grid.Row="2"
                        Grid.Column="2"
                        Padding="16,0"
                        HorizontalAlignment="Stretch"
                        Style="{StaticResource CardBorderStyle}">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Style="{StaticResource CalendarCardLabelStyle}" Text="Payment Due" />
                            <TextBlock
                                Foreground="{DynamicResource Brush.Info}"
                                Style="{StaticResource CalendarValueTextStyle}"
                                Text="{Binding PaymentDueText}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Grid>

        <Grid Grid.Column="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Style="{StaticResource CardBorderStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Style="{StaticResource CalendarSectionTitleStyle}" Text="Expenses" />
                        <customControls:FadingScrollViewer
                            Grid.Row="1"
                            Margin="0,14,0,0"
                            VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <ItemsControl
                                    ItemTemplate="{StaticResource ExpenseItemTemplate}"
                                    ItemsSource="{Binding Expenses}"
                                    Style="{StaticResource CalendarListItemsControlStyle}" />
                                <TextBlock
                                    Style="{StaticResource CalendarNoDataTextStyle}"
                                    Text="No expenses for this date."
                                    Visibility="{Binding HasNoExpenses, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                            </Grid>
                        </customControls:FadingScrollViewer>
                    </Grid>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Style="{StaticResource CalendarSectionTitleStyle}" Text="Incomes" />
                        <customControls:FadingScrollViewer
                            Grid.Row="1"
                            Margin="0,14,0,0"
                            VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <ItemsControl
                                    ItemTemplate="{StaticResource IncomeItemTemplate}"
                                    ItemsSource="{Binding Incomes}"
                                    Style="{StaticResource CalendarListItemsControlStyle}" />
                                <TextBlock
                                    Style="{StaticResource CalendarNoDataTextStyle}"
                                    Text="No incomes for this date."
                                    Visibility="{Binding HasNoIncomes, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                            </Grid>
                        </customControls:FadingScrollViewer>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Style="{StaticResource CalendarSectionTitleStyle}" Text="Goal Deadlines" />
                        <customControls:FadingScrollViewer
                            Grid.Row="1"
                            Margin="0,14,0,0"
                            VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <ItemsControl
                                    ItemTemplate="{StaticResource GoalDeadlineItemTemplate}"
                                    ItemsSource="{Binding GoalDeadlines}"
                                    Style="{StaticResource CalendarListItemsControlStyle}" />
                                <TextBlock
                                    Style="{StaticResource CalendarNoDataTextStyle}"
                                    Text="No goal deadlines for this date."
                                    Visibility="{Binding HasNoGoalDeadlines, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                            </Grid>
                        </customControls:FadingScrollViewer>
                    </Grid>
                </Border>

                <Border
                    Grid.Row="2"
                    Grid.Column="2"
                    Style="{StaticResource CardBorderStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Style="{StaticResource CalendarSectionTitleStyle}" Text="Recurring Transactions" />
                        <customControls:FadingScrollViewer
                            Grid.Row="1"
                            Margin="0,14,0,0"
                            VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <ItemsControl
                                    ItemTemplate="{StaticResource RecurringTransactionItemTemplate}"
                                    ItemsSource="{Binding RecurringTransactions}"
                                    Style="{StaticResource CalendarListItemsControlStyle}" />
                                <TextBlock
                                    Style="{StaticResource CalendarNoDataTextStyle}"
                                    Text="No recurring transactions for this date."
                                    Visibility="{Binding HasNoRecurringTransactions, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                            </Grid>
                        </customControls:FadingScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
