<UserControl
    x:Class="Fluxo.Views.Popups.Settings.Tabs.SettingsRecurringTransactionsTab"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:Fluxo.Helpers.Behaviors"
    xmlns:components="clr-namespace:Fluxo.Resources.Components;assembly=Fluxo.Resources"
    xmlns:customControls="clr-namespace:Fluxo.Resources.CustomControls;assembly=Fluxo.Resources">
    <UserControl.Resources>
        <DataTemplate x:Key="SettingsRecurringTransactionItemTemplate">
            <StackPanel>
                <Border
                    x:Name="ItemRoot"
                    Padding="12"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="1"
                    CornerRadius="14"
                    PreviewMouseLeftButtonDown="OnItemMouseLeftButtonDown">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <CheckBox
                            Margin="0,0,12,0"
                            VerticalAlignment="Center"
                            IsChecked="{Binding IsChecked, Mode=TwoWay}"
                            Style="{StaticResource ItemCheckBoxStyle}"
                            Visibility="{Binding DataContext.IsRecurringTransactionChecksEnabled, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <StackPanel Grid.Column="1">
                            <TextBlock
                                FontFamily="{StaticResource Bold}"
                                Foreground="{StaticResource Brush.Text.Primary}"
                                Text="{Binding Name}" />
                            <TextBlock
                                Margin="0,5,0,0"
                                FontFamily="{StaticResource Medium}"
                                Foreground="{StaticResource Brush.Text.Secondary}"
                                Text="{Binding TagName}" />
                        </StackPanel>
                        <StackPanel
                            Grid.Column="2"
                            Margin="16,0,0,0"
                            VerticalAlignment="Center">
                            <TextBlock
                                HorizontalAlignment="Right"
                                FontFamily="{StaticResource Bold}"
                                Foreground="{StaticResource Brush.Text.Primary}">
                                <TextBlock.ToolTip>
                                    <TextBlock>
                                        <Run Text="{Binding AmountSign, Mode=OneWay}" />
                                        <Run Text="{Binding Amount, Mode=OneWay, Converter={StaticResource MoneyFullDisplayConverter}}" />
                                    </TextBlock>
                                </TextBlock.ToolTip>
                                <Run Text="{Binding AmountSign, Mode=OneWay}" />
                                <Run Text="{Binding Amount, Mode=OneWay, Converter={StaticResource NumberWithCommasConverter}}" />
                            </TextBlock>
                            <TextBlock
                                Margin="0,5,0,0"
                                HorizontalAlignment="Right"
                                FontFamily="{StaticResource Medium}"
                                Foreground="{StaticResource Brush.Text.Muted}"
                                Text="{Binding AccountName}" />
                        </StackPanel>
                        <StackPanel
                            Grid.Column="3"
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Orientation="Horizontal">
                            <Border Style="{StaticResource StateIconBadgeStyle}" Visibility="{Binding IsEnabled, Converter={StaticResource BoolToVisibilityInvertedConverter}}">
                                <components:Icon
                                    Margin="6"
                                    Path="{StaticResource LightningSlashFill}"
                                    Color="{StaticResource Brush.Danger}" />
                            </Border>
                        </StackPanel>
                        <StackPanel
                            x:Name="RowActions"
                            Grid.Column="4"
                            Margin="8,0,0,0"
                            VerticalAlignment="Center"
                            IsHitTestVisible="False"
                            Opacity="0"
                            Orientation="Horizontal"
                            Visibility="Collapsed">
                            <customControls:BalloonButton
                                ButtonIcon="{StaticResource Delete}"
                                Click="OnRowActionClick"
                                DefaultBackground="{StaticResource Brush.BalloonButton.Background.Destructive}"
                                HoveredBackground="{StaticResource Brush.BalloonButton.Background.Destructive.Hovered}"
                                Style="{StaticResource ToolbarActionBalloonButtonStyle}"
                                Tag="RecurringTransactions:Delete"
                                ToolTip="Delete" />
                            <customControls:BalloonCheckBox
                                CheckedBackground="{StaticResource Brush.BalloonButton.Background.Caution}"
                                CheckedIcon="{StaticResource LightningSlashFill}"
                                CheckedText="Disable"
                                Click="OnRowActionClick"
                                DefaultBackground="{StaticResource Brush.BalloonButton.Background.ConfirmSave}"
                                HoveredBackground="{StaticResource Brush.BalloonButton.Background.Surface.Hovered}"
                                IsChecked="{Binding IsEnabled, Mode=OneWay}"
                                Style="{StaticResource HeaderCheckBoxStyle}"
                                Tag="RecurringTransactions:EnableToggle"
                                UncheckedIcon="{StaticResource LightningFill}"
                                UncheckedText="Enable" />
                        </StackPanel>
                    </Grid>
                </Border>
                <Border
                    Height="1.5"
                    Margin="12"
                    Background="{StaticResource Brush.Border.Subtle}" />
            </StackPanel>
            <DataTemplate.Triggers>
                <Trigger SourceName="ItemRoot" Property="IsMouseOver" Value="True">
                    <Setter TargetName="ItemRoot" Property="Background" Value="{StaticResource Brush.Background.Hover}" />
                    <Setter TargetName="ItemRoot" Property="BorderBrush" Value="{StaticResource Brush.Border.Subtle}" />
                    <Setter TargetName="RowActions" Property="Visibility" Value="Visible" />
                    <Setter TargetName="RowActions" Property="Opacity" Value="1" />
                    <Setter TargetName="RowActions" Property="IsHitTestVisible" Value="True" />
                </Trigger>
                <DataTrigger Binding="{Binding DataContext.IsRecurringTransactionChecksEnabled, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" Value="True">
                    <Setter TargetName="RowActions" Property="Visibility" Value="Collapsed" />
                    <Setter TargetName="RowActions" Property="Opacity" Value="0" />
                    <Setter TargetName="RowActions" Property="IsHitTestVisible" Value="False" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter TargetName="ItemRoot" Property="BorderBrush" Value="{StaticResource Brush.Mint}" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </UserControl.Resources>
    <Border>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    FontFamily="{StaticResource Bold}"
                    FontSize="16"
                    Foreground="{StaticResource Brush.Text.Primary}"
                    Text="Recurring Transactions" />
                <StackPanel
                    Grid.Column="1"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
                    <customControls:BalloonButton
                        ButtonIcon="{StaticResource Delete}"
                        Click="OnBatchActionClick"
                        DefaultBackground="{StaticResource Brush.BalloonButton.Background.Destructive}"
                        HoveredBackground="{StaticResource Brush.BalloonButton.Background.Destructive.Hovered}"
                        IsEnabled="{Binding HasCheckedRecurringTransactions}"
                        Style="{StaticResource ToolbarActionBalloonButtonStyle}"
                        Tag="RecurringTransactions:Delete"
                        ToolTip="Delete selected"
                        Visibility="{Binding IsRecurringTransactionChecksEnabled, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <customControls:BalloonCheckBox
                        CheckedBackground="{StaticResource Brush.BalloonButton.Background.Caution}"
                        CheckedIcon="{StaticResource LightningSlashFill}"
                        CheckedText="Disable Selected"
                        Click="OnBatchActionClick"
                        DefaultBackground="{StaticResource Brush.BalloonButton.Background.ConfirmSave}"
                        HoveredBackground="{StaticResource Brush.BalloonButton.Background.Surface.Hovered}"
                        IsChecked="{Binding ShowRecurringTransactionDisableActionButton, Mode=OneWay}"
                        IsEnabled="{Binding HasCheckedRecurringTransactions}"
                        Style="{StaticResource HeaderCheckBoxStyle}"
                        Tag="RecurringTransactions:EnableToggle"
                        UncheckedIcon="{StaticResource LightningFill}"
                        UncheckedText="Enable Selected"
                        Visibility="{Binding IsRecurringTransactionChecksEnabled, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <customControls:BalloonCheckBox
                        CheckedBackground="{StaticResource Brush.BalloonButton.Background.Surface}"
                        CheckedIcon="{StaticResource UncheckAll}"
                        CheckedText="Uncheck All"
                        Click="OnSelectionActionClick"
                        DefaultBackground="{StaticResource Brush.BalloonButton.Background.Surface}"
                        HoveredBackground="{StaticResource Brush.BalloonButton.Background.Surface.Hovered}"
                        IsChecked="{Binding AreAllRecurringTransactionsChecked, Mode=OneWay}"
                        Style="{StaticResource HeaderCheckBoxStyle}"
                        UncheckedIcon="{StaticResource CheckAll}"
                        UncheckedText="Check All"
                        Visibility="{Binding IsRecurringTransactionChecksEnabled, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <customControls:BalloonCheckBox
                        CheckedBackground="{StaticResource Brush.BalloonButton.Background.Surface}"
                        CheckedIcon="{StaticResource DisableChecks}"
                        CheckedText="Disable Selection"
                        Click="OnChecksToggleClick"
                        DefaultBackground="{StaticResource Brush.BalloonButton.Background.Surface}"
                        HoveredBackground="{StaticResource Brush.BalloonButton.Background.Surface.Hovered}"
                        IsChecked="{Binding IsRecurringTransactionChecksEnabled, Mode=OneWay}"
                        Style="{StaticResource HeaderCheckBoxStyle}"
                        UncheckedIcon="{StaticResource EnableChecks}"
                        UncheckedText="Enable Selection" />
                    <customControls:BalloonButton
                        ButtonIcon="{StaticResource PlusSolid}"
                        Click="OnAddPlaceholderClick"
                        DefaultBackground="{StaticResource Brush.BalloonButton.Background.Default}"
                        HoveredBackground="{StaticResource Brush.BalloonButton.Background.Default.Hovered}"
                        Style="{StaticResource ToolbarActionBalloonButtonStyle}"
                        Tag="Add Recurring Transaction"
                        ToolTip="Add recurring transaction"
                        Visibility="{Binding IsDashboardSpendingAmountGateLocked, Converter={StaticResource BoolToVisibilityInvertedConverter}}" />
                </StackPanel>
            </Grid>
            <TextBlock
                Grid.Row="1"
                Margin="0,0,0,16"
                FontFamily="{StaticResource Medium}"
                FontSize="11"
                Foreground="{StaticResource Brush.Text.Secondary}"
                Text="Modify, disable or remove recurring transactions" />
            <customControls:FadingScrollViewer
                Grid.Row="2"
                Margin="-12,8,-20,0"
                VerticalScrollBarVisibility="Auto">
                <ItemsControl
                    behaviors:ScrollOnDemandBehavior.HasMoreItems="{Binding HasMoreItems}"
                    behaviors:ScrollOnDemandBehavior.IsEnabled="True"
                    behaviors:ScrollOnDemandBehavior.IsLoading="{Binding IsLoading}"
                    behaviors:ScrollOnDemandBehavior.LoadMoreCommand="{Binding LoadMoreCommand}"
                    ItemTemplate="{StaticResource SettingsRecurringTransactionItemTemplate}"
                    ItemsSource="{Binding RecurringTransactionsView}" />
            </customControls:FadingScrollViewer>
            <StackPanel
                Grid.RowSpan="3"
                Margin="0,24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{Binding HasRecurringTransactions, Converter={StaticResource BoolToVisibilityInvertedConverter}}">
                <TextBlock
                    FontFamily="{StaticResource Bold}"
                    FontSize="13"
                    Foreground="{StaticResource Brush.Text.Secondary}"
                    Text="No Recurring Transactions Available" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
