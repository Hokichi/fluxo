<UserControl
    x:Class="Fluxo.Resources.Components.TimeSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Name="Root"
    Width="96"
    Focusable="True"
    PreviewKeyDown="OnPreviewKeyDown"
    PreviewTextInput="OnPreviewTextInput">

    <UserControl.Resources>
        <Style x:Key="TimeSelectorTextStyle" BasedOn="{StaticResource BaseTextBlockStyle}" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource Medium}" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
        </Style>

        <Style x:Key="TimeSegmentStyle" BasedOn="{StaticResource BaseBorderStyle}" TargetType="Border">
            <Setter Property="Padding" Value="8,0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="5" />
        </Style>
    </UserControl.Resources>

    <Border
        Height="40"
        Padding="4,0"
        Background="{DynamicResource Brush.Background.Elevated}"
        BorderBrush="{DynamicResource Brush.Border.Subtle}"
        BorderThickness="1"
        CornerRadius="8">
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Horizontal">
            <Border
                x:Name="HoursSegment"
                MouseLeftButtonDown="OnHoursSegmentMouseLeftButtonDown"
                Style="{StaticResource TimeSegmentStyle}">
                <TextBlock Style="{StaticResource TimeSelectorTextStyle}"
                    Text="{Binding FormattedHours, ElementName=Root, Mode=OneWay}" />
            </Border>
            <TextBlock
                VerticalAlignment="Center"
                FontFamily="{DynamicResource Medium}"
                Foreground="{DynamicResource Brush.Text.Muted}"
                Text=":" />
            <Border
                x:Name="MinutesSegment"
                MouseLeftButtonDown="OnMinutesSegmentMouseLeftButtonDown"
                Style="{StaticResource TimeSegmentStyle}">
                <TextBlock Style="{StaticResource TimeSelectorTextStyle}"
                    Text="{Binding FormattedMinutes, ElementName=Root, Mode=OneWay}" />
            </Border>
        </StackPanel>
    </Border>
</UserControl>
